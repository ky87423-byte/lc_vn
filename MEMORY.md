# lc_vn 프로젝트 메모리

> 마지막 갱신: 2026-06-05 / 커밋 `ec3e5d7`

## 프로젝트 정체

- **목적**: 베트남 리니지클래식(+아이온2·메이플월드·솔인챈트) 유저에게서 게임머니 매입
- **저장소**: https://github.com/ky87423-byte/lc_vn (private)
- **로컬**: `C:\Users\User\lc_vn`
- **스택**: Next.js 16.2.7 (App Router, Turbopack) + React 19 + Tailwind v4 + TypeScript. 외부 라이브러리 추가 없음 (차트도 자체 SVG)
- **자매 프로젝트**: lc_info (리니지클래식 대리육성 "데스 사관학교", 같은 스택)

## 현재 상태 (작동 확인됨)

| 기능 | 상태 |
| --- | --- |
| 4개 게임 시세표 (게임 탭) | ✅ 린클 29서버, 아이온2 44서버, 메이플월드 8월드, 솔인챈트 6서버(매물 0건) |
| 매입가 계산 | ✅ 바로템 최저가 × (1−할인율), 단위 자동 감지 |
| VND/KRW 병기 | ✅ 실시간 환율 (open.er-api.com) |
| 관리자 페이지 `/admin` | ✅ 할인율 1~30%, 갱신주기 30초~30분, 비번 `.env.local` ADMIN_PASSWORD |
| 자동 갱신 | ✅ 캐시 주기 폴링 + 탭 복귀 시 즉시, 숨김 탭 중단 |
| 24h 등락률 + 스파크라인 + 확장차트(24h/7d) | ✅ 이력 7일 보관, 백그라운드 수집기 |
| 동시접속 대응 | ✅ 100 동시요청 테스트 통과, 바로템 호출 주기당 1회 보장 |
| 다크 테마 | ✅ 시스템 설정 무관 강제 |

## 미완료 / TODO

- [ ] **연락처 교체**: `src/data/site.ts`의 zalo/facebook/kakao가 placeholder
- [ ] **브랜드명 확정**: 임시 "LC Adena VN"
- [ ] **배포**: 아직 로컬만. 배포 시 ADMIN_PASSWORD 환경변수 + `data/` 쓰기 권한 + 단일 프로세스(메모리 캐시) 확인
- [ ] SOL:enchant 매물 등장 시 화폐 단위(다이아) 표기 검증
- [ ] 아이온2 비(非)키나 매물(오드 등) 필터 — 시세 왜곡 시

## 핵심 수치 / 식별자

- 바로템 게임머니 스레드: 린클 `2382r902` / 아이온2 `2382r811` / 메이플월드 `2382r977` / 솔 `2382r1003`
- 시세 단위: 아데나 **만당** / 키나 **천만당** / 메소 **백만당** (unit_price 접두어 자동 감지)
- 기본 설정: 할인율 15%, 캐시 300초, 환율 폴백 18.5 (실측 ~17.1)
- 로컬 검증 포트: **3210** (3000은 lc_info 점유 가능)
- 관리자 비번(로컬): `lcvn2026!` — `.env.local`, git 미포함

## 마지막 검증 결과 (2026-06-05)

- 린클 데포로쥬: 시세 ₩2,450/만 → 매입 ₩2,082 = 35.700₫
- 아이온2 월드거래소(천족): 시세 ₩7,571/천만 → 매입 ₩6,435 = 110.400₫
- 메이플랜드: 시세 ₩1,702/백만 → 매입 ₩1,446 = 24.800₫
- 동시 100요청 → 전부 200, 바로템 추가 호출 0회
- 이력 수집기: 60초 간격 포인트 정상 기록 (게임별 4파일)

## 함정 (한 번 밟았던 것)

1. Tailwind v4: unlayered `body` CSS가 유틸리티를 이김 → 라이트모드 흰바탕/흰글자 사고. globals.css 다크 고정으로 해결
2. Next instrumentation ↔ 라우트 핸들러 = **별도 모듈 인스턴스** → 이력 메모리 캐시 갈라짐. 디스크 병합으로 해결
3. `gh` CLI 없음 → GitHub 저장소 생성은 `git credential fill`로 토큰 추출 후 API 호출
4. Geist 폰트는 vietnamese subset 미지원 → Be Vietnam Pro 사용
5. Bash 도구(이 환경)가 heredoc 내 `\\`를 깨뜨림 → node 스크립트에서 정규식은 `[0-9]` 등 백슬래시 없는 패턴 사용
